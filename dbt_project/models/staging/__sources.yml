version: 2
sources:
  - name: raw
    description: source tpch data
    database: snowflake_sample_data
    schema: tpch_sf1
    tables:
      - name: orders
      - name: lineitem
      - name: customer
        columns:
            - name: c_custkey 
              tests:
                - not_null

              